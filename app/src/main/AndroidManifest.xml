<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.iutassistant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".RecordsShowActivity"></activity>
        <activity android:name=".Marks" />
        <activity android:name=".AssignmentReminder" />
        <activity android:name=".ClassReschedulingReminder" />
        <activity android:name=".AnnoncementReminder" />
        <activity android:name=".Assignment" />
        <activity android:name=".ClassReshedule" />
        <activity android:name=".Quiz" />
        <activity android:name=".ShowAttendance" />
        <activity android:name=".AttendanceSelection" />
        <activity android:name=".Attendance" />
        <activity android:name=".Profile" />
        <activity android:name=".CourseSelection" />
        <activity android:name=".LogIn" /> <!-- <service -->
        <!--  -->
        <!-- android:name=".MyMessage" -->
        <!-- android:enabled="true" -->
        <!-- android:exported="true" -->
        <!-- &gt;</service> -->
        <service android:name=".MyMessage">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".Message" />
        <activity android:name=".UniversitySelectingClass" />
        <activity
            android:name=".TeachersHomePage"
            android:label="@string/title_activity_teachers_home_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AdminLoginActivity" />
        <activity
            android:name=".home_page_student"
            android:label="@string/title_activity_home_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Welcome" />
        <activity android:name=".splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signup_student" />
        <activity android:name=".MainActivity" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>